#include<iostream.h>
#include<conio.h>
void main()
{
int num, sum=0, rem;
cout<<"Enter any number:=";
cin>>num;
while(num>0)
{
rem=num%10;
sum=sum+rem;
num=num/10;
}
cout<<"Sum of digits:="<<sum;
getch();
}
